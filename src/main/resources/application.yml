
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/soon_project
    username: soon
    password: soon
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
  config:
    import:
      - classpath:keys.yml